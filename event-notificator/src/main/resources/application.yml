spring:
  application:
    name: event-notificator
  datasource:
    driver-class-name: org.postgresql.Driver
  kafka:
    bootstrap-servers: kafka:9092

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

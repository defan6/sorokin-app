spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/dev_profiles_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
  kafka:
    bootstrap-servers: localhost:9093
server:
  port: 7084
file-storage:
  url: http://localhost:7085
